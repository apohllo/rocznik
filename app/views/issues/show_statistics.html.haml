.scroll-panel-header
  = link_to "Wróć do numeru", issue_path, class: %w{btn btn-default btn-sm pull-right}
  %h3.scroll-panel-title
    Statystyki do numeru
    = @issue.title
.scroll-panel-body
  -if @issue.prepared?
    %table.table.table-hover.table-admin
      %thead
        %tr
          %th Liczba artykułów w j.ang.
          %th Liczba artykułów
          %th Udział procentowy %
      %tbody
        - issues.each do |issue|
          %tr
            %td= issue.submissions.english.count
            %td= issue.submissions.count
            %td= issue.submissions.english.count / issue.submissions.count * 100
