.scroll-panel-header
  = link_to "Anuluj", @article_revision.submission, class: %w{btn btn-default btn-sm pull-right}
  %h3.scroll-panel-title
    = @article_revision.title
.scroll-panel-body
  = simple_form_for @article_revision do |f|
    = f.input :accepted, as: :boolean, label: "Zatwierdź"
    = f.input :comment, label: "Komentarz"
    = f.submit "Zapisz", class: %w{btn btn-primary btn-outline}
