.scroll-panel-header
  %h3.scroll-panel-title Numery rocznika
.scroll-panel-body
  .row.text-center{:style => "float: center;"}
    - @issues.each do |issue|
      .panel.panel-primary.col-md-3.margin{:style => "margin: 0 10px 20px 10px"}
        .panel-heading
          %h3.panel-title= issue.title
        .panel-body
          %a{"data-refer" => public_issue_path(issue)}
            %h2
              %span= icon('book', class: "fa-3x")
          %div Liczba artykułów: #{issue.articles.count}

:javascript
  $('.panel-primary').click(function(){
    var ref = $(this).find('a').data('refer');
    window.location.href = ref;
  });