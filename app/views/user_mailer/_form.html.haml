=simple_form_for @simplemail do |f|
  = f.input :addressee, label: "Do:"
  = f.input :subject, label: "Tytuł"
  = f.input :body, label: "Treść"
  = f.input :sender, label: "Od"
  - label = @simplemail.new_record? ? "Wyślij"
  = f.submit label, class: %w{btn btn-primary btn-outline}

